% this script is called by 'save_trait_mat'
basal_level = basal_level(:);
ind_level = ind_level(:);
modality = modality(:);
basal_frac = basal_frac(:);
ind_frac = ind_frac(:);
mask_basal = mask_basal(:);
mask_induction = mask_induction(:);
gluc = gluc(:);
galc = galc(:);